import React from 'react';
import './Loading.css';

const Loading = () => {
  return (
    <div className="center-loader">
      <span className="loader"></span>
    </div>
  );
};

export default Loading;
